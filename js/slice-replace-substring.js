const producto = "Monitor 60 pulgadas";

console.log(producto);
// . replace para reemplazar
console.log(producto.replace("pulgadas", "'"));

//slice para cortar
console.log(producto.slice(0, 9));

//Alternativa a slice
console.log(producto.substring(0, 10));
